This directory is intentionally tracked for clean cross-platform test builds.

The release build script generates and verifies the platform-specific native
inference worker beneath this directory before Tauri packages the application.
This marker is not an executable and contains no model or user data.
