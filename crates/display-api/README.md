# Display API

Owns the display-safe privacy projection contract. The current slice is a pure Rust transformation with no listener, socket, credential, provider adapter, or desktop command. Network serving and pairing remain separate fail-closed gates.
