# Status

## Completed

- Repository and architecture foundation created.
- Full 1,318-line product specification received and reviewed.
- Milestone 1 desktop shell, local database, settings implementation, and architecture baseline created.
- Rust domain/database tests pass (4 tests); TypeScript strict typecheck and npm audit pass.
- Apple Silicon release binary and ad-hoc-signed `Personal Assistant.app` generated and verified.
- Production React/Vite assets are now embedded in the generated Tauri application; the temporary static packaging fallback was removed.
- Reproducible Node 20/22 LTS macOS test/build/DMG script added.
- Milestone 1 DMG installation check passed on the user's Mac; Milestone 1 closed.
- Milestone 2 foundation: Microsoft native OAuth authorization-code flow with PKCE and loopback callback.
- macOS Keychain secret-store adapter; account metadata and synchronization cursors remain in SQLite without tokens.
- Graph client with TLS-only requests, trusted continuation URL validation, throttling signals, and metadata-only inbox/sent delta URLs.
- Microsoft account connection/status UI and database migration 2 added.
- Refresh-token rotation, independent inbox/sent delta synchronization, transactional cursor persistence, and rolling calendar read synchronization implemented.
- Calendar event schema migration, account disconnect, optional cascading local-data deletion, and Sync Now desktop controls implemented.
- Provider fixture coverage verifies metadata-only delta payloads, deletion tombstones, cursor commits, and account-data cascade behavior.
- Graph retry policy now honors bounded `Retry-After`/exponential delays for throttling and transient server failures.
- Per-account synchronization is single-flight and safely cancellable from the desktop UI.
- Sanitized sync-run diagnostics persist outcome and item counts without message content, tokens, URLs, or provider error text.
- The complete-email-content preference is present and defaults OFF; metadata-only persistence remains enforced until encrypted body storage is implemented.
- Background Microsoft synchronization runs every 15 minutes while the app is active, with missed-tick delay and the existing per-account single-flight guard.
- Last-sync outcome/counts are visible without exposing provider content or errors.
- Explicit downloaded-data deletion removes mail metadata, calendar events, cursors, and sync history while preserving the connected account and Keychain credential.
- Retention maintenance removes provider deletion tombstones after 30 days and sanitized sync diagnostics after 90 days.
- Rust test suite now passes 14 tests; React strict typecheck, production build, formatting, and warning-free Clippy pass.
- Microsoft Entra multi-tenant/public-account application registration created for Personal Assistant.
- Desktop loopback redirect `http://localhost/oauth/callback` configured and public-client flows enabled.
- Public application client ID embedded as the production default; `PA_MICROSOFT_CLIENT_ID` remains available as a build-time override.
- Microsoft-enabled Apple Silicon release `.app` rebuilt, ad-hoc signed, and verified as a valid arm64 application bundle.
- Live Microsoft sign-in completed for an organisational/personal-account capable registration; profile metadata and the connected account were persisted successfully.
- First live Graph synchronization completed successfully with independent inbox and sent delta cursors and calendar retrieval. The connected account returned zero current items, verifying the empty-result path without persisting message content.
- Second live Graph synchronization completed successfully using the persisted inbox and sent delta cursors; both cursor success timestamps advanced and no errors were recorded.
- Milestone 2 exit criteria passed: live connect and repeatable incremental sync work, credentials remain outside SQLite, and Graph data is not routed to an external AI service.
- Milestone 3 slice 1: dedicated Rust `ai` service crate, native macOS RAM/CPU/free-storage detection, Apple Metal capability reporting, deterministic compact/standard tier recommendation, and desktop capability UI implemented.
- Private-model download remains intentionally disabled pending a pinned signed catalog, bounded downloader, SHA-256 verification, and atomic installation; its supply-chain threat model is documented.
- Milestone 3 slice 1 validation passes: 18 Rust tests, strict TypeScript, production Vite build, formatting, and warning-free Clippy.
- Apple Silicon release `.app` rebuilt with the capability UI, ad-hoc signed, and verified as a valid arm64 bundle.
- Milestone 3 slice 2: immutable Apache-2.0 Qwen3 compact/standard catalog, TLS host allowlist, bounded redirects/size/time, resumable staging, SHA-256 verification, atomic promotion, scoped removal, progress UI, and single-download guard implemented.
- Slice 2 validation passes: 21 Rust tests, strict TypeScript, production frontend build, formatting, and warning-free Clippy.
- Model-lifecycle Apple Silicon `.app` rebuilt in an isolated target, copied to `outputs`, ad-hoc signed, and verified as a valid arm64 bundle.
- Live download exposed Hugging Face's current `us.aws.cdn.hf.co` artifact redirect; that exact official TLS host was added to the narrow allowlist with a negative lookalike-host test.
- Redirect fix validation passes: 22 Rust tests, strict TypeScript, production frontend build, formatting, and warning-free Clippy; the corrected arm64 `.app` was rebuilt, signed, verified, and copied to `outputs`.
- Live compact-model lifecycle validation passed: Qwen3 1.7B Q8 installed at the exact pinned 1,834,426,016-byte length, its full SHA-256 matched the catalog/marker, and no partial staging file remained.
- Milestone 3 runtime gate: official llama.cpp macOS ARM64 build b10434 and its dependent libraries are embedded with licence/provenance metadata; no separate Python, Node, Docker, Ollama, or runtime installation is required.
- Runtime health checks independently verify the complete model digest and pinned runtime build before a bounded one-token load, prefer Apple Metal, fall back to CPU safely, expose no listener, and receive no email content.
- Runtime integration validation passes the Rust workspace tests and warning-free Clippy; direct restricted-runner validation confirms the CPU fallback path while Metal command queues remain unavailable in that sandbox.
- The runtime-enabled Apple Silicon `.app` was rebuilt, its embedded ARM64 runtime/version manifest verified, the complete bundle ad-hoc resealed, strict deep signature verification passed, and the corrected single-turn packaged CPU health invocation exited successfully.
- Packaged-app testing exposed that llama.cpp emits `--version` on stderr. Version validation now safely combines stdout/stderr while still requiring a successful process and exact build 10434; regression coverage was added, 24 Rust tests pass, and the corrected `.app` was rebuilt and strictly verified.
- Milestone 3 structured extraction contract implemented for the full classification taxonomy, urgency, tasks, appointments, and waiting-for candidates with strict JSON, bounded content, finite confidence, and exact source-evidence validation.
- Prompt-injection containment encodes untrusted input and escapes delimiter characters; unknown action-like fields and fabricated evidence are rejected.
- The dedicated deterministic `rules` crate now converts validated extraction only into inert suggestions, Review, or no-action plans; low confidence, critical urgency, and contradictory classifications are forced to Review.
- Validation now passes 32 Rust tests, warning-free Clippy, strict TypeScript, and the production frontend build. Provider email remains intentionally disconnected from inference.
- The extraction/rules slice Apple Silicon `.app` was rebuilt with pinned llama.cpp build 10434, ad-hoc resealed, and passed strict deep signature verification.
- Fixture-only local inference now uses deterministic, bounded GBNF-constrained generation. The pinned runtime's JSON-Schema conversion sampler regression was reproduced with the documented empty schema; direct GBNF works and remains backed by the strict application JSON contract.
- Evidence offsets are no longer delegated to the model. The model returns an exact quote; Rust requires one unique source occurrence and derives UTF-8 byte offsets, rejecting fabricated and ambiguous evidence.
- The first real compact-model fixture gate passes end-to-end on CPU fallback: payment classification, two grounded tasks, no false appointment/waiting candidates, exact evidence, and no external service.
- A four-case evaluation corpus now covers payment/document tasks, prompt injection, meeting rescheduling, and waiting-for detection. Validation passes 38 Rust tests, warning-free Clippy, strict TypeScript, and the production frontend build.
- The constrained-fixture slice Apple Silicon `.app` was rebuilt with embedded llama.cpp build 10434, ad-hoc resealed, and passed strict deep signature and ARM64 architecture verification.
- Deterministic temporal resolution now handles explicit ISO dates, today/tomorrow, named weekdays, 12/24-hour times, and trusted RFC 3339 reference offsets. Vague/invalid dates, missing appointment starts, and reversed ranges are forced to Review.
- A synthetic-only desktop evaluation command and UI control run all four fixtures locally and return only case IDs/pass counts. Model integrity is verified once before the corpus and Microsoft-synced content remains unreachable from this command.
- Validation passes 45 Rust tests, warning-free Clippy, strict TypeScript, and the production frontend build.
- The temporal/evaluation Apple Silicon `.app` was rebuilt, ad-hoc resealed, and passed strict deep signature, pinned-runtime-version, and ARM64 architecture verification.
- First packaged Metal corpus run completed safely at 1/4; the release gate remained closed and live provider-email inference stayed disabled.
- Failed cases were identified as prompt injection, meeting rescheduling, and waiting-for detection. Known injection language is now conservatively rejected before inference and counts as a pass only through the explicit safety-rejection path.
- Synthetic failure diagnostics now expose only observed classification and task/appointment/waiting counts; generated text and fixture content remain hidden. Generation is capped at 512 tokens.
- Validation passes 46 Rust tests, warning-free Clippy, strict TypeScript, and the production frontend build.
- The injection/diagnostic Apple Silicon `.app` was rebuilt after system load normalized, ad-hoc resealed, and passed strict deep signature, pinned-runtime-version, and ARM64 verification.
- Second packaged Metal corpus run scored 2/4: payment extraction and prompt-injection safety rejection passed; meeting-reschedule and waiting-for were rejected before a validated result.
- Sanitized failure codes now distinguish timeout, framing, JSON, evidence, contract, model-integrity, and runtime failures without exposing fixture/model content. Validation passes 47 Rust tests.
- Failure diagnostics identified meeting-reschedule as a structurally valid but semantically incorrect payment result with extra task/waiting candidates; waiting-for-reply failed exact evidence validation. Prompt tuning targets those concrete errors without relaxing evidence or quality gates.
- Final packaged Apple Metal corpus run passed 4/4: payment/document tasks, prompt-injection safety rejection, meeting rescheduling, and waiting-for detection all met their unchanged semantic and provenance thresholds.
- Milestone 3 isolated-worker foundation: a dedicated Rust worker executable now communicates only through inherited stdin/stdout pipes using a versioned four-byte length-prefixed JSON protocol. Frames are capped at 128 KiB, request IDs are constrained, malformed/version-mismatched input is rejected, shutdown is explicit, and there is no network listener or filesystem logging.
- Worker extraction fails closed with `backend_unavailable` until the exact llama.cpp b10434 C headers and persistent model backend are linked; the existing fixture-only `llama-cli` path and the provider-data boundary are unchanged.
- Validation now passes 53 Rust tests, warning-free workspace Clippy, strict TypeScript, a production Vite build, and npm audit with zero known vulnerabilities.
- The exact upstream b10434 C header and simple-reference source were independently retrieved from commit `7e4c0a968`; their SHA-256 digests are pinned in the worker ABI provenance record. The header confirms by-value ABI structures, so unsafe hand-authored approximations are prohibited.
- The desktop supervisor policy now deterministically enforces one active job, at most eight pending jobs, pre-start deadline rejection, queued cancellation, termination-required active cancellation, and at most three crash restarts per stability window. Eleven worker tests pass.
- The b10434 ABI probe is now compiled into the worker through a warning-as-error C build shim. It carries the official Apple Silicon structure-size contract and dynamically verifies every llama.cpp symbol required by persistent generation before model access; the bundled `libllama.0.1.0.dylib` passes the probe.
- Fourteen worker tests now pass, the native ARM64 release worker rebuilds and passes strict signature verification, and the workspace test total is 61.
- A persistent model/context prototype successfully loaded the pinned 1.7B model, but real generation exposed that a size-equivalent mirrored C declaration is not ABI-safe: Metal failed cleanly in the restricted runner and the CPU decode path aborted internally. The prototype is quarantined behind a disabled experimental Cargo feature and is not linked into the default worker or desktop application.
- The complete seven-header llama.cpp/ggml C API set from pinned commit `7e4c0a968` is now vendored. The Rust build script verifies every SHA-256 before compiling, and the native shim directly includes the official headers; all mirrored by-value ABI declarations were removed.
- Exact-header testing identified and fixed two native-loop defects: the CPU device list now has backend lifetime rather than stack lifetime, and `llama_sampler_sample` is no longer followed by an invalid duplicate accept. The persistent CPU run now generates without aborting but exceeded the restricted runner's practical validation window; the feature remains disabled pending packaged Apple Metal validation.
- The exact-header persistent worker is now built as a native ARM64 executable and embedded under the signed application's private resources. It is inactive by default, has no Tauri command or provider-data route, and requires parent-supplied runtime/model environment paths before its experimental backend can initialize.
- The packaged worker passed a real length-prefixed pipe health exchange. The complete Apple Silicon `.app` was rebuilt, the nested worker and app were ad-hoc resealed, strict deep signature verification passed, and both application and worker executables verify as ARM64 with llama.cpp build 10434 unchanged.
- A synthetic-only Tauri command now supervises the packaged persistent worker: it is single-flight, re-verifies the installed model, supplies only trusted runtime/model paths, uses bounded pipe frames and per-case deadlines, suppresses native stderr, and forcibly terminates the child on completion or timeout.
- The Settings UI can run the unchanged four-case corpus through one persistent model process and displays only sanitized case identifiers, pass counts, and stable failure metadata. Microsoft-synchronized records remain unreachable from this command.
- The supervisor-enabled Apple Silicon `.app` was rebuilt with the experimental persistent backend confined to the child worker. All 61 workspace tests, strict TypeScript, production Vite build, formatting, and warning-free Clippy pass; the nested worker and complete app pass strict deep-signature and ARM64 verification.
- The legacy packaged `llama-cli` corpus remains 4/4, but the first packaged persistent-worker corpus scored 1/4. Prompt-injection rejection passed; permission/payment, meeting-reschedule, and waiting-for-reply failed. The persistent release gate therefore remains closed and provider inference remains disabled.
- Persistent evaluation failure presentation now includes only the same sanitized classification, candidate counts, and stable failure category used by the legacy evaluator; source text and generated model output remain hidden.
- Sanitized diagnostics showed schema-valid but semantically weak direct-Metal results: zero candidates for the permission/payment and reschedule cases, and two waiting candidates for one awaited response. The general prompt checklist now requires requested obligations, exactly one rescheduled appointment, and deduplication of a single awaited reply without weakening evidence or schema validation.
- A generic second-pass repair proved too ambiguous for the compact model and was replaced by one classification-specific repair with a separate semantic-consistency gate. The first packaged run of that path exceeded the original 180-second single-pass deadline, so the synthetic gate now has a hard 420-second per-case deadline plus five seconds of parent-process shutdown margin; retries remain capped at one and timeout still terminates the worker.
- The persistent native backend now applies the verified model's embedded chat template through pinned llama.cpp APIs and uses Qwen's local `/no_think` control. This corrected the raw-prompt semantic instability and raised the packaged Metal result to 3/4.
- Explicit “waiting for/awaiting a reply or response” language is handled by a narrow deterministic extractor rather than unstable generation. It derives byte offsets and exact evidence directly from the source, produces one waiting candidate, and does not activate for generic phrases such as “waiting room”.
- The final packaged Apple Metal persistent-worker corpus passed 4/4. The persistent worker gate is closed successfully: payment/document action extraction, injection safety rejection, meeting rescheduling, and waiting-for detection all satisfy the unchanged evaluation thresholds.
- Validation now passes 66 workspace tests, 15 feature-enabled worker tests, warning-free Clippy, strict TypeScript, and the production frontend build. The published app and nested worker are signed ARM64 executables and pass strict deep verification.
- Production-supervisor slice 1 adds a bounded private-work queue that owns request metadata and payload together. It rejects duplicate IDs, preserves FIFO payload alignment while discarding expired jobs, supports targeted queued/active cancellation, caps pending work at eight, and redacts all payloads from `Debug` output.
- Queue validation raises the workspace total to 70 tests; the feature-enabled worker passes 19 tests and warning-free Clippy. The qualified worker remains disconnected from provider content while the process adapter is integrated.
- Production-supervisor slice 2 adds the process adapter that exclusively owns the bounded queue and isolated child. It performs correlated health/extraction exchanges, kills the worker on active cancellation or deadline, rejects late/mismatched responses, never retries a failed active payload, and limits crash recovery to three restarts per 60-second window.
- Four real-subprocess lifecycle tests pass for FIFO promotion, cancellation kill, deadline kill, and restart-budget exhaustion. The workspace now passes 80 tests in total; the feature-enabled worker passes 29 tests. Warning-free Clippy, formatting, the strict frontend build, and npm audit also pass.
- The adapter-routed packaged Apple Metal persistent-worker corpus passed 4/4, preserving the qualified worker gate through the production process boundary.
- Provider activation slice 1 adds a durable qualification record bound to the exact model SHA-256, pinned runtime build, and corpus version, plus a separate default-off local-email-analysis consent. Consent cannot be enabled against a missing or stale qualification, and removing the model revokes qualification and consent atomically. No mailbox body retrieval or automatic provider inference was added.
- Database migration 6 and two qualification/revocation tests raise the workspace total to 82 tests. Strict TypeScript, production frontend build, formatting, warning-free Clippy, and npm audit remain clean.
- Transient-provider slice 1 adds a dedicated Microsoft Graph message-content request for future local analysis. Provider IDs are path-segment encoded and bounded; redirects are disabled; only the required fields are selected; plaintext is explicitly requested; declared and streamed response sizes are capped; HTML, mismatched IDs, malformed JSON, and oversized bodies fail closed.
- Sensitive transient subject/body allocations have payload-redacted `Debug` output and are zeroed on drop. Three provider-boundary tests raise the workspace total to 85. The method is not called by synchronization or exposed to the webview, so automatic body retrieval and provider inference remain disabled.
- Reviewed-handoff slice 1 adds migration 7 and transactional sanitized analysis persistence. Stored records contain validated summary, classification, urgency, confidence, inert selected suggestion fields, review reasons, and model digest; evidence quotes, byte offsets, and message bodies are structurally absent. A regression test inspects SQLite and proves a private evidence excerpt is not retained, raising the workspace total to 86 tests.
- Reviewed-handoff slice 2 replaces plain worker-source strings with a redacted zeroizing owner. Queue copies, protocol request copies, outbound serialized JSON buffers, and inbound framed JSON buffers are scrubbed on drop. Request-level debug redaction has dedicated regression coverage, raising the workspace total to 87 tests; real provider handoff remains disabled.
- Reviewed-handoff slice 3 connects one explicitly selected synchronized Microsoft message at a time to the qualified local worker. The UI shows metadata for at most ten recent messages and requires per-message confirmation. The backend rechecks consent, source-message ownership, exact qualification, complete model integrity, and Keychain credentials before bounded transient retrieval.
- Provider analysis is globally single-flight, uses the isolated worker health/deadline/cancellation lifecycle, revalidates returned extraction against the zeroizing source copy, applies deterministic rules, and transactionally stores only the evidence-free analysis record. Background synchronization remains metadata-only and no automatic inference was enabled. All 87 workspace tests, 30 feature-worker tests, frontend, Clippy, formatting, and audit gates pass.
- Fixed the Microsoft sync UI to refresh account status immediately after a successful sync, exposing newly persisted recent-message metadata and its explicit “Analyse privately” action without requiring an application restart. The replacement Apple Silicon app passed strict TypeScript, production frontend build, ad-hoc signing, and strict deep bundle verification.
- Replaced the unreliable webview-native confirmation used by “Analyse privately” with an accessible in-app confirmation panel. Provider content remains untouched until the second explicit confirmation; analysis progress, sanitized success, and backend failures remain visible inline. The corrected Apple Silicon bundle passes frontend validation and strict deep signature verification.
- Live provider handoff passed on 22 August 2026: one synchronized Microsoft message was explicitly confirmed, transiently retrieved, classified locally as School, routed to Review with one inert suggestion, and persisted with the sanitized summary “finding info contained”. SQLite inspection confirmed the record has only derived fields, review metadata, and a 64-character model digest; the schema contains no body, evidence, or source-offset columns.
- Milestone 4 Review slice 1 adds a read-only sanitized Review query and accessible list/detail interface. It presents subject metadata, summary, classification confidence, urgency, human-readable review reasons, and inert task/appointment/waiting suggestions; it exposes no provider body, evidence, offsets, provider links, or executable actions. A dedicated privacy regression raises the workspace total to 88 tests. Full workspace tests, warning-free Clippy, formatting, strict TypeScript, production frontend build, ARM64 packaging, and strict deep signature verification pass.
- Milestone 4 Review slice 2 adds explicitly confirmed Accept and Ignore decisions with migration 8 and an on-device audit history. Decisions are terminal and idempotent per analyzed message; a repeated matching decision succeeds without duplication, while a conflicting rewrite fails closed. Accept records acknowledgement only and cannot execute tasks, calendar changes, replies, or provider writes. Saved analyses are now immutable through both UI and backend to preserve decision provenance. All 88 workspace tests, warning-free Clippy, formatting, strict TypeScript, production build, ARM64 packaging, and deep signature verification pass.
- Milestone 4 local-projection slice adds migration 9 and local-only To Do, Waiting For, and Calendar views. Accepted sanitized suggestions are projected idempotently with account/message/suggestion provenance and category; the migration backfills existing accepted decisions. Undo is an explicit confirmed, non-destructive transition to an `undone` state and preserves both the projection and Review audit history. No provider write scopes or Graph mutations were added. All 88 workspace tests, Clippy, formatting, strict TypeScript, production build, ARM64 packaging, and deep verification pass.
- Milestone 4 lifecycle slice adds migration 10, validated local Complete/Snooze/Reschedule transitions, explicit in-app confirmation, RFC 3339 schedule normalization, client idempotency keys, and append-only audit events containing before/after lifecycle state and schedule. Existing projections migrate to Active without automatic action. Completed items leave active views; snoozed/rescheduled items retain local scheduling; projection Undo remains non-destructive. No Keychain or provider access occurs. All 88 workspace tests, Clippy, formatting, strict TypeScript, production build, ARM64 packaging, and strict deep verification pass.
- Milestone 4 focus-view slice adds read-only Today, Work, Kids, and Personal tabs over the single local projection source. Rust validates an explicit local-day window and fixed category mappings: Work; School/Creche/Kids; Personal/Home. Today uses effective scheduled/due/start/follow-up time, and all views exclude undone or completed records. No data duplication, inference, synchronization, or mutation occurs on tab changes. All 88 workspace tests, Clippy, formatting, TypeScript, production build, ARM64 packaging, and strict deep verification pass.
- Milestone 4 provenance slice adds sanitized local-item source details and an explicit Open source email action. IPC exposes only provider, subject, sender, occurrence time, and availability—never the provider URL or body. Rust resolves ownership through local item/account/message joins, requires an undeleted synchronized source, and opens only credential-free HTTPS URLs on exact Outlook hosts (`outlook.office.com`, `outlook.office365.com`, `outlook.live.com`) using the default browser. Lookalikes, HTTP, embedded credentials, and nonstandard ports fail closed. The new security regression raises the workspace total to 89 tests; all tests, Clippy, formatting, TypeScript, production build, ARM64 packaging, and deep verification pass.
- Milestone 4 Home slice adds a compact read-only dashboard with exact active To Do/Waiting/Calendar counts, exact pending Review count, up to five Today highlights, and sanitized account sync health. It reuses local projection, focus, Review, account, and sync-run data; completed/undone records are excluded and dashboard refresh never initiates sync, inference, or mutation. All 89 workspace tests, Clippy, formatting, strict TypeScript, production build, ARM64 packaging, and strict deep verification pass.
- Milestone 4 navigation slice adds persistent accessible section navigation for Home, Focus, Review, Assistant, Accounts, Settings, and Private AI. Only the selected workspace panel is rendered, Review carries a pending-count badge, and narrow-window horizontal overflow, dark mode, and reduced-motion preferences are supported. This presentation-only slice adds no data access or permissions. All 89 workspace tests remain clean; strict TypeScript, production build, ARM64 packaging, and strict deep signature verification pass.
- Milestone 4 exit review passed. Required Home, Today, To Do, Waiting For, Calendar, and Review experiences are working, with accessible navigation, explanation, local lifecycle, focus, provenance, and dashboard support. Draft reply remains deliberately deferred to Milestone 5's correspondence-confirmation safeguards, so Milestone 4 closes without adding provider write permissions.
- Milestone 5 automation foundation adds the validated Conservative, Balanced (default), and Assistant policy preference through Rust, SQLite, IPC, and Settings UI. Migration 11 adds a constrained append-only audit ledger with database triggers rejecting update and deletion. No executor reads the policy and no automatic action is enabled. The workspace now has 91 native tests; sequential tests, formatting, warning-free Clippy, strict TypeScript, production frontend build, ARM64 packaging, and strict deep signature verification pass.
- Milestone 5 policy-engine slice adds a pure deterministic decision matrix for task creation, calendar creation/update, correspondence, and notifications. It evaluates sensitivity, confidence, verified source, confirmed facts, reversibility, explicit user confirmation, and the selected policy. Invalid/unverified input blocks; low-confidence or unconfirmed calendar facts require review; correspondence and sensitive actions always require confirmation. Results are inert and no executor or provider adapter is connected. Five matrix regressions raise the workspace total to 96 native tests; sequential tests, formatting, warning-free Clippy, TypeScript, production build, ARM64 packaging, and strict deep verification pass.
- Milestone 5 inert-queue slice adds migration 12 and an offline action-proposal store with immutable records, one-to-one audit/idempotency keys, exact-replay behavior, conflict rejection, bounded sanitized display metadata, opaque local target references, and explicit RFC 3339 expiry capped at 30 days. Active queries omit expired proposals without deleting history. Blocked policy decisions are audited but never queued, and the schema contains no raw body/content/payload/evidence/provider URL. No Tauri command, executor, provider scope, or automatic action was added. Four database regressions raise the workspace total to 100 native tests; sequential tests, formatting, warning-free Clippy, TypeScript, production build, ARM64 packaging, and strict deep verification pass.
- Milestone 5 proposal-lifecycle slice adds migration 13 with one append-only confirm/cancel event per inert proposal and a matching immutable audit row. State is derived as Pending, Confirmed, Cancelled, or Expired without rewriting proposal history; exact replay is idempotent, conflicting terminal decisions fail closed, and expired proposals cannot be decided. The Assistant view lists bounded proposal metadata and uses an explicit confirmation panel that states no action will execute. No executor or provider permission was added. Two database regressions raise the workspace total to 102 native tests; sequential tests, formatting, warning-free Clippy, TypeScript, production build, ARM64 packaging, and strict deep verification pass.
- Milestone 5 deterministic proposal-factory slice connects explicit accepted Review decisions to the inert queue. Verified appointments create calendar-create proposals; dated or locally scheduled tasks/waiting items create reminder proposals; undated items create none. Stable keys derive from local item IDs, expiry from immutable creation time, sensitivity from urgency/category, and labels from bounded sanitized titles. Replays are idempotent and the Assistant queue refreshes after acceptance. No executor, provider write scope, or raw content path was added. All 102 native tests, formatting, warning-free Clippy, TypeScript, production build, ARM64 packaging, and strict deep verification pass.
- Milestone 5 notification-foundation slice replaces the reserved crate with typed Reminder, Urgent Alert, Morning Summary, and Evening Summary planning; privacy classes/content modes; bounded validated text; one-year scheduling horizon; overnight quiet-hours handling; urgent bypass; and an idempotent recording sink. Work-private and sensitive content must be generic. Migration 14 adds default-off notification preferences and validated 22:00–07:00 quiet hours to local Settings. No permission request or delivery adapter exists. Six regressions raise the workspace total to 108 native tests; sequential tests, formatting, warning-free Clippy, TypeScript, production build, ARM64 packaging, and strict deep verification pass.
- Milestone 5 notification-preview slice adds a read-only planner over active local items and default-off preferences. It previews appointment reminders up to seven days ahead, high/critical urgent alerts, and generic morning/evening counts; completed/undone items are excluded, Work/Sensitive content is generic, and quiet-hours shifts are shown. IPC and Assistant UI label all output Preview only. No native plugin, permission request, or delivery command exists. All 108 native tests, formatting, warning-free Clippy, TypeScript, production build, ARM64 packaging, and strict deep verification pass.
- Milestone 5 native-notification permission slice links official Tauri notification plugin 2.3.3 with a Rust-1.88-compatible notify-rust pin. Capabilities expose only permission status/request; the webview cannot choose or deliver notification content. One explicit native command sends a fixed generic test after writing an idempotent append-only audit event. Automated scheduling remains disconnected. All 109 native tests, formatting, warning-free Clippy, strict TypeScript, production frontend build, ARM64 packaging, and strict deep signature verification pass.
- Live testing showed the desktop plugin reports permission as granted unconditionally and can silently suppress its legacy NSUserNotification delivery while the app is foreground. macOS now uses the modern UserNotifications framework directly: real authorization status/request, awaited submission errors, and a retained foreground Banner/List/Sound delegate. Webview notification capabilities were removed entirely; fixed content, auditing, and the disconnected scheduler remain unchanged. All 109 native tests, formatting, warning-free Clippy, strict TypeScript, production frontend build, ARM64 packaging, and strict deep signature verification pass.
- Native test delivery now uses the default notification sound, applies bounded timeouts to every macOS callback, and queries Notification Center for the exact request identifier after submission. The UI distinguishes confirmed delivery from acceptance without a visible receipt and from rejection/timeout. This diagnostic build passes the same 109-test, formatting, Clippy, frontend, ARM64 package, and deep-signature gates.
- Live packaged-app validation passed: macOS authorization was detected correctly and the fixed generic foreground notification was received successfully. The native permission/test-delivery gate is closed.
- Migration 15 adds separate automatic-local-delivery consent through the Rust domain, SQLite, IPC, and Settings UI. It defaults off and the control is unavailable without real macOS permission. Saving consent still cannot schedule anything because the scheduler remains deliberately disconnected. The 109-test workspace, formatting, warning-free Clippy, strict TypeScript, production frontend build, ARM64 packaging, and strict deep signature verification pass.
- The scheduler foundation now has a pure deterministic reconciliation function. It schedules only missing desired keys, cancels stale installed keys, rejects duplicate desired identifiers, and is idempotent across restart snapshots. One regression raises the workspace total to 110 native tests. No OS scheduling call is connected yet.
- Migration 16 adds an append-only, content-free scheduling event ledger with constrained event types/kinds/timestamps/reason codes, exact-replay idempotency, conflict rejection, and update/delete triggers. Its schema contains no notification text or source-email fields. One regression raises the workspace total to 111 native tests; targeted database and notification tests pass. Native scheduling remains disconnected.
- The managed macOS scheduler is connected behind real authorization plus migration 15 consent. Every minute it reconciles validated local plans with pending UserNotifications requests, schedules missing future plans, retains installed plans, cancels stale plans after revocation, records stable SHA-256-keyed ledger events, and treats past scheduled keys as satisfied to prevent repeat urgent alerts. A fixed generic one-minute test verifies native persistence without caller-controlled content. The workspace now passes 113 native tests, strict TypeScript, production Vite, formatting, warning-free Clippy, ARM64 packaging, and strict deep signature verification.
- Live packaged-app validation passed for the fixed generic one-minute scheduled notification. The app now reconciles immediately when Settings are saved and exposes only a content-free count of pending automatic macOS requests; isolated diagnostic requests are excluded. A regression raises the workspace total to 114 native tests; strict TypeScript, production Vite, formatting, warning-free Clippy, ARM64 packaging, and strict deep signature verification pass.
- Live packaged-app validation passed for consent revocation: enabling delivery produced a pending automatic request and disabling consent immediately reduced the pending count to zero. The automatic-notification gate is closed.
- Calendar execution-readiness slice 1 replaces delegated `Calendars.Read` with least-privilege `Calendars.ReadWrite` and adds a disconnected Graph create-event adapter. It accepts only a fixed `/me/events` endpoint, bounded validated confirmed event data, UTC-normalized start/end values, and a stable transaction ID for provider duplicate suppression. No Tauri command, UI execution control, or caller-selected URL is connected. Two regressions raise the workspace total to 116 native tests; full workspace tests, formatting, warning-free Clippy, ARM64 packaging, and strict deep signature verification pass.
- The packaged OAuth build was reconnected successfully with delegated calendar-write consent. Migration 17 adds immutable, content-free calendar execution preparation and terminal-result ledgers with unique proposal and transaction identifiers, exact replay, and update/delete triggers. Preparation now requires a confirmed, unexpired calendar-create proposal backed by an active local appointment, validates bounded title and time data, and fails on conflicting retries. Terminal recording accepts exactly one succeeded, failed, or unknown outcome; exact replay is safe and conflicting outcomes fail closed. The schema contains no title, body, email content, payload, or evidence fields. One regression raises the workspace total to 117 native tests; full workspace tests, formatting, and warning-free Clippy pass. Provider invocation remains disconnected.
- Calendar-create execution is connected only through an explicit second confirmation shown for an already locally confirmed calendar proposal. The command refreshes the delegated token, submits one fixed-endpoint request with the prepared transaction ID, records success only with a returned provider event ID, records provider rejection as failed, and records transport ambiguity as unknown without retry. No background service invokes it; calendar updates and correspondence remain unavailable. All 117 native tests, strict TypeScript, production frontend, formatting, warning-free Clippy, ARM64 packaging, and strict deep signature verification pass.
- The first live preparation failed safely before Graph because locally extracted appointment timestamps omitted an offset. Read-only database inspection confirmed the proposal, confirmation, target, lifecycle, and range were otherwise valid. Preparation now normalizes naïve wall times through the Mac's system timezone, emits explicit offsets, and rejects nonexistent or ambiguous DST times. The targeted regression, formatting, warning-free Clippy, corrected ARM64 package, and strict deep signature verification pass; no provider event was created by the failed attempt.
- The retry then exposed an unfinished-preparation recovery gap: the immutable ledger contained the original execution/transaction ID without any terminal result, while the UI generated a new key. Recovery now reuses the preserved transaction ID for an unfinished attempt, blocks keys owned by another proposal, and continues to reject any terminally completed attempt. The command records its result against the recovered execution key. Recovery regression, formatting, warning-free workspace Clippy, strict TypeScript, production build, corrected ARM64 packaging, and deep signature verification pass; the audit history was not rewritten or deleted.
- Live packaged-app validation passed for calendar creation. The explicitly double-confirmed proposal created the Microsoft event exactly once, and unfinished-attempt recovery safely reused the original transaction ID. The calendar-create execution gate is closed.
- Calendar-update readiness slice 1 adds migration 18 to retain Microsoft event ETags during read-only calendar sync and a disconnected update adapter. PATCH is pinned to the encoded Graph `/me/events/{id}` endpoint, requires `If-Match`, accepts only bounded start/end changes, normalizes UTC, and cannot alter title, body, attendees, recurrence, or caller-selected URLs. One adapter regression raises the workspace total to 118 native tests; targeted database/email tests, formatting, and warning-free Clippy pass. No update IPC or UI path exists.
- Live read-only sync captured the calendar-create test event and a non-empty Microsoft ETag. Migration 19 adds an immutable, content-free update-target snapshot, and candidate selection exposes only active synchronized events with an ETag; cancelled or concurrency-unprotected events are excluded. One regression raises the workspace total to 119 native tests; targeted tests, formatting, and warning-free Clippy pass. The desired replacement time still requires explicit user input before proposal creation.
- Calendar-update execution is now connected behind two explicit confirmations. The first creates an inert immutable old/new time proposal; the second invokes a fixed-endpoint Graph PATCH only if the synchronized ETag is unchanged. Execution preparation is recoverable before a terminal result, terminal outcomes prohibit retry, HTTP 412 is recorded as stale, and ambiguous transport results stop without automatic retry. The selected live test time is 28 August 2026, 11:00–11:15. All 119 native tests, strict TypeScript, production Vite, formatting, and warning-free Clippy pass. The fresh Apple Silicon app and nested worker were ad-hoc resealed, verified as ARM64, passed strict deep signature verification, and were copied to `outputs/Personal Assistant.app` for live qualification.
- Live packaged-app calendar-update qualification passed. The explicitly proposed and double-confirmed test changed the Microsoft event exactly once from 10:00–10:15 to 11:00–11:15 Dublin time. Follow-up synchronization returned one event, stored 10:00–10:15 UTC, captured a new 36-character ETag distinct from the immutable precondition snapshot, and the append-only ledger contains exactly one `succeeded/provider_accepted` result. The calendar-update gate is closed.
- Correspondence foundation slice 1 adds a disconnected reply-only Graph adapter pinned to the encoded source-message `/reply` endpoint. It accepts no recipient or arbitrary URL, validates and caps plaintext at 16 KiB, redacts content from diagnostics, zeroizes request buffers, and has no IPC execution command or `Mail.Send` permission. Migration 20 stores only immutable source targeting plus append-only content digest/byte-count revisions; draft plaintext is held under a dedicated macOS Keychain service and zeroized after native storage. Explicit local-data deletion removes Keychain entries before metadata. The Accounts UI can save one private reply draft and then shows `Draft saved`; sending remains disabled. The workspace now passes 120 native tests, strict TypeScript, production Vite, formatting, and warning-free Clippy.
- Live packaged private-draft qualification passed after exercising recovery from an unavailable first Keychain item. Replacement appends a revision under the same immutable source target, immediately reads the new value back, verifies SHA-256 and byte count, and zeroizes the read buffer. The user then received `Verified 1 private reply draft in macOS Keychain`; read-only SQLite inspection shows two content-free audit revisions, a latest size of 44 bytes, 64-character digests, and no body/content/subject/recipient/sender column. Sending remained disabled throughout. The private-draft storage gate is closed.
- Replacing ad-hoc-signed builds exposed that macOS binds Keychain access to the changing CDHash, making draft secrets unavailable after each update. Identifier-only ad-hoc requirements were explicitly rejected as insecure. A local self-signed code-signing identity named `Personal Assistant Development` now provides a stable certificate-anchored designated requirement; a scoped signing script verifies the identity, known bundle/worker paths, ARM64 architecture, deep signature, and published output. The certificate-signed app passes strict deep verification. Developer ID remains mandatory for production distribution.
- Live inert correspondence confirmation passed. The stable-signed app read and integrity-verified the latest 11-byte Keychain revision before creating a sensitive, confirmation-required proposal. SQLite captured only revision key/digest targeting; the user explicitly recorded confirmation and read-only inspection verified the matching append-only `confirmed/user_confirmed_inert` audit event. No send button, `Mail.Send` scope, or provider mutation existed. The inert confirmation gate is closed.
- Disconnected correspondence execution readiness is complete. Migration 22 adds immutable content-free preparation and terminal-result ledgers, requires a confirmed unexpired proposal backed by a still-present synchronized source and exact latest draft revision, recovers only an unfinished execution key, and blocks retry after any terminal outcome. Regression coverage includes preparation, unfinished recovery, terminal conflict, stale-revision rejection, immutability, and privacy-schema checks. The full workspace passes 120 native tests; warning-free Clippy also passed in the completed validation run. Provider sending remains disconnected.
- Correspondence provider activation is implemented behind delegated `Mail.Send`, the already-confirmed inert proposal, and a separate immediate send confirmation. The route is pinned to the source message's `/reply` endpoint, accepts no recipients, revalidates Keychain digest and byte count, zeroizes reply buffers, records succeeded/failed/unknown exactly once, and never retries transport ambiguity. All 120 native tests, warning-free workspace Clippy, strict TypeScript, production Vite, and the ARM64 `.app` build pass. The restricted runner cannot access the local development signing identity, so publication and live qualification are pending the user-run signing script.
- The first correspondence execution qualification stopped before Graph because the selected private draft revision was absent from Keychain. The append-only ledger records exactly one terminal `failed/draft_missing` result and no success; retry is correctly prohibited. Terminal correspondence proposals are now surfaced as `closed` rather than remaining visually sendable. Recovery requires a new Keychain revision, new proposal, and new confirmations; the failed history will not be rewritten.
- A second controlled revision passed the synchronous Keychain verification but execution's asynchronous worker-side Keychain lookup timed out after two minutes and returned missing; Graph was again never called and the attempt was terminally closed. Reply-secret retrieval is now marshalled onto the native app main thread, matching the proven save/verify path, before asynchronous token refresh and provider I/O. Plaintext remains inside native memory, is not returned to the webview, and retains digest/size revalidation and zeroization.
- Live correspondence qualification passed with a newly saved and independently verified 19-byte Keychain revision. The confirmed proposal executed exactly once, Graph returned acceptance, and the immutable ledger records one `succeeded/provider_accepted` terminal result. Follow-up synchronization retrieved a distinct sent-message record timestamped immediately after execution with the expected reply subject; no retry or duplicate execution exists. Outlook had not yet surfaced the item in its visible Sent Items view, but provider metadata proves the sent record exists. The correspondence execution gate is closed successfully.
- Post-gate usability slice adds a read-only content-free Correspondence history showing bounded local labels, terminal outcome, sanitized reason, and timestamp; reply text, recipients, source content, provider IDs, and execution keys are absent. Terminal failures explain that nothing was sent, successful acceptance is explicit, and ambiguity warns that retry was blocked. Confirmation panels are now centered, scroll-bounded dialogs instead of appearing below the fold. The workspace remains at 120 passing native tests with warning-free Clippy, strict TypeScript, production Vite, and a fresh ARM64 `.app` bundle.
- Summary-time correctness now supplies the Mac's local RFC 3339 offset to both native background reconciliation and webview previews instead of UTC. Morning/evening plans therefore use local wall time (including Dublin summer offset at planning time) while retaining quiet-hours, privacy, idempotency, and delivery-consent gates.
- Morning summaries now count items scheduled on the summary's local calendar day, while evening summaries count the following local day; the misleading rolling 24-hour window was removed. A read-only Automation policy history exposes only policy, decision, action kind, sanitized reason, and timestamp. Event keys, target references, provider identifiers, and content are not returned to the webview. The combined slice passes all 120 native tests, warning-free Clippy, strict TypeScript, production Vite, and a fresh ARM64 app bundle.
- Live summary/audit qualification passed. The installed app presented content-free automation history and previewed the next morning summary at `2026-09-01T07:30:00+01:00`, proving local Dublin offset handling; the preview reported zero local items and performed no provider write.
- Policy-driven local projection is implemented. Balanced policy can project an all-high-confidence, routine, reversible suggestion plan directly into undoable local items; Conservative, important, sensitive, tentative-calendar, low-confidence, and mixed plans remain wholly in Review. Every evaluated suggestion receives a content-free append-only policy audit entry, and no provider I/O is available from this path.
- Migration 23 safely changes local-item provenance from a manual Review decision to the underlying validated analysis, preserving all existing item identifiers, lifecycle state, scheduling, and events. Migration 24 extends the immutable audit vocabulary to record `eligible` and `confirmation_required` faithfully instead of misrepresenting policy outcomes.
- Review now includes unprojected `suggestions` plans and supplies a sanitized policy-decision explanation when the extraction itself had no review reason. Successfully projected plans are excluded from Review and immediately refresh local task/calendar views.
- Validation passes 121 native tests, warning-free workspace Clippy, strict TypeScript, and the production Vite build.
- The policy-projection Apple Silicon application was rebuilt and signed with the stable `Personal Assistant Development` identity. Both the desktop executable and isolated inference worker are ARM64; the published bundle passes strict deep signature verification and its certificate-anchored designated requirement.
- Live packaged migration qualification passed: the signed policy-projection build launched from Applications and preserved the existing account and local data. Milestone 5 is closed.
- Milestone 6 slice 1 replaces the reserved `display-api` crate with a pure, disconnected privacy projector. `PUBLIC_FAMILY` may retain policy-approved display-safe titles; `PRIVATE` and `WORK_PRIVATE` are capped at generic even if a future malformed policy requests full details; `SENSITIVE`, missing categories, and hidden policies emit no item. The serialized type structurally omits provider, account, email, source, body, sender, and subject fields.
- The dedicated Family Display threat model now treats the Raspberry Pi and home LAN as untrusted. It requires encrypted authenticated local transport, explicit opt-in, no public binding, five-minute single-use rate-limited pairing, independent 256-bit read-only credentials, one-way host storage, immediate revocation, bounded no-store responses, and separate future gates for touch actions, discovery, weather, DAKboard, and remote access. No network listener exists.
- The disconnected pairing state machine generates six-digit five-minute rendezvous codes, locks after five failed attempts, consumes successful codes once, returns a redacted/zeroizing random 256-bit credential, and derives its SHA-256 host record. Migration 25 stores only the digest plus bounded per-display policy/metadata and append-only paired/revoked events. Authentication is display-scoped, constant-time over valid digests, updates only content-free last-seen time, and fails immediately after revocation.
- The read-only display snapshot contract is schema-versioned, supports Today/Week/Month/Morning/Evening modes, caps responses at 100 already-projected items, requires an offset-bearing generation time, and contains no action/command/URL surface.
- Milestone 6 foundation validation passes 129 native tests and warning-free workspace Clippy. The family-display client, server socket, desktop controls, and all data egress remain disconnected.
- The separate React/TypeScript Family Display client now builds as its own workspace application. It validates every versioned snapshot at runtime, rejects oversized/unknown data, requests only a same-origin no-store endpoint, has a self-only CSP with no forms/frames/remote connections, keeps no offline cache, and exposes no controls or navigation.
- The kiosk presentation implements large-distance typography, landscape schedule/reminder panels, responsive portrait/touch layouts, category markers, and an explicit privacy-preserving offline state. Browser QA passed at 1920×1080 and 800×1280; layout bounds remain within the configured viewports. Both desktop and display strict typechecks and production Vite builds pass, and npm reports zero known vulnerabilities.
- The Pi deployment boundary is specified: an owner-only native launcher—not Chromium—will hold the display credential and pinned TLS trust, proxy the authenticated read-only Mac connection, and serve the static UI exclusively on Pi loopback. The browser CSP therefore remains unable to contact the Mac or Internet directly.
- Migration 26 assigns every existing and future local item a conservative display privacy profile in a separate constrained table: family/school/crèche/kids/home default public-family, work defaults work-private, and all other derived categories default private. Explicit changes append an immutable privacy event. Per-display snapshot generation reads only active local items, applies the stored display policy, replaces database IDs with display-specific SHA-256 identifiers, and returns at most 100 already-filtered items.
- Desktop Settings now contains a read-only Family Displays management panel backed by native commands. It lists only bounded display name/status/last-seen metadata and can explicitly revoke one active display; pairing and service activation remain unavailable until authenticated TLS is implemented. The webview never receives a display credential or token digest.
- Validation now passes 130 native tests, warning-free workspace Clippy, both strict TypeScript applications, both production Vite builds, and npm audit with zero known vulnerabilities.
- The Milestone 6 management/migration Apple Silicon candidate builds successfully with the isolated ARM64 inference worker embedded. Stable local signing and packaged migration qualification require the user-held development identity.
- The candidate was signed with the stable `Personal Assistant Development` identity and published to `outputs`. Independent verification confirms both executables are ARM64 and the complete bundle passes strict deep signature verification plus its certificate-anchored designated requirement.
- Live packaged qualification passed: the migration preserved existing account/local data and Settings showed the expected `No family displays are paired` state. No listener was opened and no pairing secret existed.
- The native Raspberry Pi launcher crate now validates and atomically installs its future pairing bundle. It accepts only HTTPS on private/loopback IPs or exact `personal-assistant.local`, fixed port 8765, no URL credentials/query/fragment, a bounded display ID/token, and an exact certificate SHA-256 pin. Configuration directories must be owner-only, files are mode 0600, symlinks and unsafe permissions fail closed, rotation is same-directory atomic, and token/config buffers are zeroized. Debug output redacts both token and certificate pin. No connection or listener is active.
- Full validation now passes 132 native tests and warning-free workspace Clippy.
- The Raspberry Pi native client transport is implemented but remains disconnected. Pairing configuration now carries the host certificate DER plus its SHA-256 fingerprint; the launcher verifies they match before atomically storing them. HTTPS disables public roots and redirects, uses only the fixed `/api/v1/snapshot` GET route, authenticates by bearer header, requires JSON, streams at most 128 KiB, and revalidates the complete display contract after deserialization.
- A real ephemeral TLS integration test verifies certificate-pinned connection, fixed request routing, credential transport, and successful strict snapshot decoding. Unknown response fields, hidden items, invalid timestamps/identifiers/titles, digest mismatches, and oversized data fail closed. The launcher now passes 4 tests and warning-free Clippy.
- The disconnected Mac host request boundary now accepts only bodyless `GET /api/v1/snapshot`, requires both a display-scoped ID and bearer credential, hashes the transient token for constant-time digest authentication against SQLite, and projects only after successful authentication. POSTs, action routes, request bodies, missing/wrong credentials, and revoked identities fail closed. Credentials and display identity are redacted from request diagnostics. Two host regressions pass, bringing the workspace total to 136 native tests; targeted Clippy is warning-free.
- The opt-in TLS listener boundary is implemented but remains disconnected from desktop startup. It refuses disabled, wildcard, public, and non-8765 binds; generates a native self-signed identity for the selected private host; caps headers, connection time, concurrency, tracked sources, per-source rate, and global rate; rejects bodies, transfer encoding, duplicate credentials, and mutation routes; and returns uniform content-free failures. Deterministic shutdown aborts and joins in-flight work.
- A real TLS integration connects the production listener to the certificate-pinned launcher and authenticates through the database before returning a strict projection. Six display-server regressions and four launcher regressions pass, with warning-free targeted Clippy. The workspace total is now 140 native tests.
- Migration 27 adds a singleton, default-off Family Display service configuration and append-only enable/disable audit events. SQLite stores only the selected private bind address and certificate fingerprint—never the TLS private key, certificate body, display token, or pairing bundle. Private/wildcard/public/non-8765 addresses fail validation. One database regression raises the workspace total to 141 tests.
- Desktop service activation is implemented behind an explicit in-app confirmation. Native code generates or reuses a fingerprint-addressed identity, stores only its private key in the dedicated macOS Keychain service, keeps the public certificate in an owner-only atomic file, verifies both on recovery, and starts the listener only after all checks pass. Address rotation is prohibited while any display remains active; integrity failure never silently rotates trust.
- Enabled service state restores at application startup only when the saved address, certificate digest, owner-only certificate file, Keychain private key, and TLS keypair all validate. The UI receives only enabled/running/address/fingerprint status and can explicitly stop the listener. Two native storage/fingerprint regressions bring the workspace total to 143 tests; desktop Clippy and the strict production frontend build pass.
- The Apple Silicon release application builds successfully with both native executables verified ARM64 and was signed by the user-held `Personal Assistant Development` identity.
- Live activation passed on `192.168.68.102:8765`. Independent inspection confirmed the exact private listening address, migration 27, a 32-byte SQLite fingerprint with no private key, and a matching live TLS certificate digest. Exact unauthenticated snapshot, unknown-route, and POST probes all returned the same empty 404. Restart restoration remains the final listener qualification check.
- Live restart restoration passed with the same `85d035981901…` certificate prefix and the service automatically returned to Running. The desktop listener gate is closed.
- Pairing now has a native coordinator shared by the desktop command and TLS listener. Settings can create only a six-digit, five-minute challenge; the strict `/api/v1/pair` route accepts only a bounded display name and code, locks after five failures, registers a digest-only 256-bit credential once, and zeroizes its one-time response. The credential never enters either React webview.
- The native Family Display executable implements `pair` and `serve`. Pairing verifies the host certificate and fingerprint before storing its config atomically; serving binds only `127.0.0.1:4173`, proxies only validated snapshots, serves only allowlisted bounded assets, emits a self-only CSP, and retains no offline snapshot. The React display refreshes once per minute and clears to an explicit offline state on failure.
- Full validation passes 146 native tests, warning-free workspace Clippy, both strict production frontend builds, and npm audit with zero known vulnerabilities.
- Live pairing emulation consumed one challenge exactly once and created a 776-byte mode-0600 config; SQLite retained only the 32-byte credential digest. The first authenticated request updated `last_seen_at` but failed projection safely because a legacy appointment used a naïve wall time. Display projection now normalizes only unambiguous system-local legacy times to explicit RFC 3339 offsets and omits invalid or ambiguous timestamps. Its regression passes, and a corrected ARM64 app has been rebuilt.
- After signing the correction, the existing credential fetched a live HTTP 200 schema-v1 snapshot without re-pairing. It contained two display-safe items (one policy-approved full, one generic, zero hidden) and exactly the allowed top-level/item fields; scans found no provider, account, email, sender, subject, body, source, action, command, or URL keys. The loopback UI returned the required self-only CSP and no-store headers. The snapshot/pairing data-flow gate is closed.
- Live cleanup exposed that display revocation still depended on the unreliable webview `window.confirm` API. It has been replaced with the established accessible in-app confirmation dialog while retaining the same immediate digest revocation command and append-only audit event. The corrected production frontend and ARM64 app build successfully and await signing for cleanup qualification.
- Live revocation qualification passed for `Emulated Kitchen Display`. Its existing credential immediately received an empty HTTP 503 through the Pi proxy, and its local credential plus all captured snapshot/header artifacts were deleted. The host retained only the revoked digest and append-only audit history.
- Raspberry Pi OS ARM64 packaging is implemented as a reproducible Debian package workflow. The package contains the native launcher and built UI, a dedicated unprivileged system account, owner-only credential storage, hardened systemd service, loopback-only Chromium kiosk autostart, and a root pairing helper that drops privileges before handling the credential. No Python, Node, Docker, Ollama, or Rust installation is required on the Pi.
- A native ARM64 GitHub Actions job builds, tests, packages, and publishes the `.deb` as a CI artifact. Local shell policy validation and the production Family Display build pass; an actual Linux ARM64 package build remains the packaging qualification gate.
- Packaging now rejects anything except a Linux ARM64 ELF launcher, preventing accidental shipment of the locally built macOS ARM64 executable. The root pairing helper copies the public certificate into an ephemeral owner-only file before dropping to the dedicated service account, so enrollment does not depend on the source file's permissions. Shell syntax, package-policy checks, production TypeScript/Vite build, and the negative Mach-O rejection test pass.
- Pre-publication validation passes all 147 native tests, warning-free workspace Clippy, both strict TypeScript checks, both production web builds, and the Family Display package policy gate.
- Native Linux ARM64 CI qualification passed on GitHub Actions in 1m21s for commit `f10c9fd`. The published 1.18 MB artifact digest is `d118d9850a52ef8592dbe267b76da19d7ccd64f15499048df9e3a5f2a1937275` and independently matches the authenticated download.
- Independent Debian inspection passes: package metadata declares arm64 and the intended dependencies; the launcher is a stripped dynamically linked Linux AArch64 ELF; maintainer scripts and launchers are executable; systemd and autostart files have non-executable data permissions; the installed tree contains only the launcher, read-only UI, pairing/kiosk helpers, service, and autostart entry; and scans found no token, client-secret, or private-key material.
- Desktop-guided Pi bootstrap now returns the configured HTTPS host, complete SHA-256 fingerprint, and Base64 public DER certificate only while a live pairing challenge exists. The certificate is re-read solely from its owner-only non-symlink file after digest verification; the Keychain private key is never accessed or serialized. A tamper regression raises the native total to 148 tests, and desktop Clippy, strict TypeScript, and the production build pass.
- The enrollment candidate was rebuilt and signed with `Personal Assistant Development`. Both native executables are ARM64, the bundle passes strict deep signature verification and its certificate-anchored designated requirement, and the embedded command contract contains the new bounded bootstrap fields. Live display of the bootstrap values remains the final Milestone 6 gate.
- Guided Pi enrollment now returns the configured private-LAN HTTPS URL, exact certificate SHA-256, and Base64 public DER certificate alongside the five-minute pairing challenge. The certificate is read only from the owner-only non-symlink file after digest verification; the Keychain private key is not accessed or serialized. The Settings UI provides bounded, wrap-safe setup values and Raspberry Pi decoding instructions.
- The enrollment slice passes 148 native tests including its tamper regression, warning-free desktop Clippy, strict TypeScript, and the production frontend build. A new Apple Silicon `.app` candidate contains ARM64 desktop and worker executables; its expected unsigned resource seal awaits stable development signing.
- The enrollment build was signed with `Personal Assistant Development`, passed strict deep signature and designated-requirement verification, and retained ARM64 desktop/worker executables. Live Applications testing confirmed all three setup values render correctly. Milestone 6 is complete.

## Currently working

- Milestone 8 selected; DAKboard was skipped. Release foundation work is implementing deterministic SBOM generation, immutable dependency-review CI, least-privilege hardened-runtime signing, notarisation/stapling verification, and explicit updater/backup safety gates.
- Milestone 8 release foundation now includes a deterministic CycloneDX 1.5 generator covering 706 unique locked Rust/npm components plus the inference-runtime manifest digest; official CI actions are pinned to immutable upstream commits; dependency review, npm audit, full tests, warning-free Clippy, and SBOM publication are defined in CI.
- Production macOS operations now have explicit no-JIT/no-unsigned-memory entitlements and a fail-closed Node 22 release script. It requires a Developer ID Application identity and notarytool Keychain profile, builds the app before the DMG, signs nested Mach-O code and the app with hardened runtime/timestamps, creates the Applications-shortcut DMG, notarizes, staples, Gatekeeper-assesses, and hashes it. No credential fallback exists.
- The encrypted-backup crate implements a bounded versioned container using fixed-cost Argon2id and XChaCha20-Poly1305 with header authentication, random salt/nonce, internal database SHA-256, strict manifest parsing, SQLite signature checks, and password/key/plaintext zeroization. Three regressions cover randomized round-trip plus wrong passwords, tampering, truncation, unsupported versions, weak passwords, invalid timestamps, and non-SQLite input.
- Live backup export is now connected behind a native save dialog and explicit twice-entered 12-character-minimum recovery password. SQLite's online backup API creates a consistent private mode-0600 snapshot without copying WAL files; Argon2 encryption runs off the UI thread; the final `.pabackup` is created without overwrite through a same-directory atomic hard-link boundary; plaintext staging is removed after every outcome. The password is never persisted and UI fields are cleared after each attempt. Restore remains disabled.
- Backup/export validation raises the native total to 153 tests. New coverage verifies consistent snapshot readability, private permissions, no overwrite, private encrypted output, container randomization/authentication, tamper/wrong-password rejection, strict size/format checks, and password zeroization. Targeted warning-free Clippy, both strict TypeScript checks/builds, and npm audit pass.
- The encrypted-export Apple Silicon `.app` candidate was resealed with the stable `Personal Assistant Development` identity and published to `outputs`. Independent verification confirms both embedded executables are ARM64, the bundle passes strict deep signature verification and its certificate-anchored designated requirement, and the packaged executable contains the encrypted-backup command contract. Live export passed with a 389,402-byte owner-only container; independent inspection confirmed its expected authenticated-envelope header and found no plaintext SQLite, schema, or private-content markers.
- A read-only verification flow accepts an explicitly selected `.pabackup`, rejects symlinks, streams from an already-open regular file under a fixed size cap, authenticates it, checks its internal digest, exact schema version, and SQLite quick integrity, and removes the private transient database on every outcome. It returns only database size and a no-restore statement. The complete workspace has 154 native tests with warning-free Clippy, both strict TypeScript builds, zero high-severity npm findings, and a regenerated 706-component SBOM.
- The verification-enabled Apple Silicon `.app` was signed with the stable `Personal Assistant Development` identity and published to `outputs`. Independent verification confirms both bundled executables are ARM64, the bundle identifier remains `com.pattobin.personal-assistant`, the complete bundle passes strict deep signature and certificate-anchored designated-requirement checks, and the packaged command contract contains the sanitized verification result. Live read-only verification is the remaining backup preflight gate.
- Live read-only verification passed against the exported container: 389,120 authenticated database bytes passed digest, schema, and SQLite integrity validation, and the application explicitly reported that no data was restored. The export/verification preflight gate is closed. Destructive restore remains disabled pending rollback-safe restart qualification.
- GitHub's ARM64 Family Display workflow passed for release-foundation commit `ea22191`. Dependency/SBOM CI is now configured for every `main` push as well as pull requests and manual dispatch, closing the branch-validation trigger gap.
- The first main-branch dependency/SBOM run passed checkout, npm installation, and the high-severity npm audit, then failed when the Ubuntu runner reached the Rust desktop build without Tauri's native Linux development libraries. The workflow now installs the documented WebKitGTK, AppIndicator, SVG, and packaging prerequisites before Cargo; a rerun is pending.
- The prerequisite-corrected run reached Cargo but exposed a pre-existing cross-platform test defect: a macOS-only fingerprint decoder was referenced by an unguarded desktop unit test. The test is now correctly target-gated; production code is unchanged.
- The next Ubuntu run exposed the underlying portability boundary: desktop code imports the platform vault abstraction while its `MacKeychain` implementation previously did not exist off macOS. A non-macOS compatibility implementation now compiles but fails every secret operation closed as unavailable; no insecure fallback or file-based credential storage is introduced. Native Windows Credential Manager remains a future platform adapter.
- Because unauthenticated GitHub APIs expose only Cargo's exit code, CI now converts only the bounded final 3.5 KB of Rust test or Clippy output into a sanitized GitHub error annotation. This contains build diagnostics only, enables remote failure triage, and does not collect or upload application data.

## Blocked

- Release signing/notarisation requires an Apple Developer ID and notarisation credentials supplied through secure CI, never committed to the repository.
- DMG regeneration is unavailable inside the current restricted runner because macOS disk-image attachment is blocked; the previously verified DMG pipeline remains unchanged and can be run in a normal macOS session.

## Next milestone

- Milestone 8: validate the release foundation in CI, then implement encrypted backup/restore before the separately signed automatic updater. Production macOS distribution requires Developer ID signing/notarisation credentials supplied through secure release infrastructure.
